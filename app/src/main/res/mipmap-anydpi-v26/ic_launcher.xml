<?xml version="1.0" encoding="utf-8"?>
<!-- The icon already has its own rounded background, so we use it directly
     rather than wrapping in adaptive-icon which would double-round it. -->
<bitmap xmlns:android="http://schemas.android.com/apk/res/android"
    android:src="@drawable/sefira_icon"
    android:gravity="fill" />
